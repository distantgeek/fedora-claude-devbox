# Rootless Podman storage configuration for open-atomic
# Deployed to ~/.config/containers/storage.conf via image skel
# Uses overlay driver with metacopy for efficient layer storage.

[storage]
driver = "overlay"

[storage.options.overlay]
mountopt = "nodev,metacopy=on"
